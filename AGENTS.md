# AGENTS.md — AI Pet 原型目录

> 适用路径：`D:/Home_Work/prototype/`。本目录用于产品、交互与视觉评审，不是业务应用的生产代码仓。

## 开工前必读

1. 先读 `D:/Home_Work/AGENTS.md`，遵守其中的安全看板同步、协作、契约与密钥规则。
2. 再读 `D:/Home_Work/work_dashboard/AI-Pet项目全景与进度.md`（第一信息源）和 `D:/Home_Work/work_dashboard/AI-Pet协作看板.md`，确认各端真实进度；看板工作树不干净时不得擅自 pull 或提交他人改动。
3. 涉及用户端页面时，读 `D:/Home_Work/ai-pet-app/AGENTS.md`、`ai-pet-app/docs/03-信息架构与页面规格.md`、`ai-pet-app/docs/05-对接API与数据流.md`。
4. 涉及接口状态时，以 `D:/Home_Work/ai-pet-backend/docs/06-HTTP-API规范.md` 为唯一契约真源；不要以原型示例数据替代接口事实。

## 目录职责与可改范围

本目录的交付物为无需构建、可在本地浏览器直接打开的静态原型与产品分析文档：

- `index.html`：AI Pet 原型总览与 P0–P8 页面评审。
- `next-step.html`：下一轮独立交互原型；当前为“绑定后的人设初始化”。
- `需求分析与下一步原型方案-2026-08.md`：需求、优先级、依赖与待定契约。
- `design/`：已确认的视觉概念稿。
- `README.md`：本地查看说明与原型边界。

可修改以上文件，或新增同类静态 HTML、CSS、JavaScript、Markdown 和设计资产。除非用户明确要求，**不得**在本目录的原型任务中修改 `ai-pet-app`、`ai-pet-backend`、`ai-pet-admin`、`xiaozhi-server` 或固件源码。

## 原型红线

1. 原型全部使用演示数据；不得请求生产 API、写入真实账号/设备数据或携带密钥。
2. 必须明确区分“已上线 / 等待后端 / 等待硬件或服务”。未实现能力只能展示为目标态或诚实空态，不能伪装为当前可用。
3. 用户端只调用业务后端公开用户 API；原型不得设计 App 直连 MQTT、实时语音、设备 MCP、KB 运营或管理台功能。
4. 语音交互发生在实体设备与 `xiaozhi-server` 之间；App 只展示设备状态，不直接控制眼睛、舵机或其他外设。
5. 人设修改的正确承诺是“下次实体设备会话开始时生效”：backend 写入后，`xiaozhi-server` 拉取 `persona_pack`。不得承诺即时下发。
6. 不新增社交、排行榜、Feed、星盘计算或重型养成系统，除非需求文档和版本范围先行更新。

## 设计与实现约定

- 文案和注释用中文；代码标识符可用英文。
- 原型保持无 CDN、无外部依赖、双击 HTML 即可查看；优先 inline CSS/JS。
- 延续消费级 PWA 视觉系统：柔和白底、星云紫、清晰状态、低干扰圆角；避免做成开发者仪表盘。
- 新增页面应至少包含：评审目标、目标用户状态、核心操作、成功/空态或等待态、返回总览入口。
- 修改信息架构、状态文案或接口依赖时，先更新 `需求分析与下一步原型方案-2026-08.md`，再改 HTML，并同步 README 的页面说明。

## 验收与收工

1. 用本地静态服务器或直接打开 HTML 做浏览器检查：无控制台错误、链接可返回、主要点击交互可用、手机宽度可阅读。
2. 每次至少核对：页面标题、核心 CTA、状态文案、空态/错误态、移动端布局。
3. 如原型结论改变了跨仓范围、接口契约或版本优先级，只记录需求事实，并提示负责仓处理；不要越权修改其他仓契约。
4. 若完成的是会影响全项目排期的正式需求决策，再按根 `AGENTS.md` 更新协作看板；纯视觉试验或未确认方案不写看板。

## Git 收工约定

- 用户已授权：本目录中由当前任务产生、且已完成验收的改动，默认在收工时执行 `git add`、`git commit` 与 `git push`。
- 提交前仍须先查看 `git status` 和 diff；如发现与当前任务无关或归属不明的改动，不得擅自提交，应先向用户确认。
- 提交信息使用简短中文，推送到已配置的 `origin` 当前分支；除非用户明确要求，不创建 Pull Request。
