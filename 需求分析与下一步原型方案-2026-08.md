# AI Pet 需求分析与下一步原型方案

> 日期：2026-08-02。依据 `D:\Home_Work` 五仓源码、`work_dashboard` 项目进度、用户端/后端 API 契约与固件规划整理。

## 结论

产品当前最短价值闭环是：**注册登录 → 绑定已上线设备 → 初始化人设 → 下一次实体宠物会话带入人设**。用户端不承担实时语音、KB 运营或硬件 MCP 控制；它是宠物主人查看和管理“关系资产”的界面。

现阶段不应以“养成数值”“实时陪聊”“社交”作为首页主叙事。它们没有相应的稳定数据源，且会掩盖 V0.2 的真正成功标准：5 分钟完成认领和人设初始化。

## 已核实的能力与原型处理

| 域 | 当前事实 | 原型策略 |
|---|---|---|
| 账号 | `auth/register`、`auth/login`、`auth/me` 已接入用户端 | 登录作为真实可完成入口 |
| 设备 | bind/list/detail/改名/解绑已上线；用户端 B2 尚未实现 | 绑定为主 CTA；首页显示设备已连接的目标态 |
| 人设 | 后端 persona 与 persona_pack 仍是骨架，用户端 P3 仅本地交互 | 保留初始化流程和“下次会话生效”，保存状态须标示等待接口 |
| 对话历史 | 旁路写入已完成；用户查询 messages 尚未实现 | 原型展示按日浏览/删除的目标态，开发中先用空态 |
| 记忆 | memories 与 MCP 仍未实现 | 候选审核为目标态；上线前不可显示伪造真实数据 |
| 小记/日运 | analyses 与 worker 处理器未实现 | 只做“等待生成”空态，不做星盘计算或虚构日运 |
| 外设 | 内部快照上报已规划/部分接入；用户查询 API 未实现 | 只读状态页，禁止在 App 直接调 MCP |
| 社交 | V0.3 设计，BLE/服务端/固件均未完成 | 不放进 V0.2 IA |

## 用户流程与状态

```text
未登录 → 注册/登录 → 无设备
                       ↓
                 配网说明 → 输入/扫码 device_uid → 绑定成功
                                                     ↓
                         人设未初始化 ─→ 直选 / 问卷 ─→ 保存
                                                     ↓
                              下次实体设备会话开始时，xiaozhi 拉取 persona_pack
```

关键规则：设备离线时仍可保存人设与记忆；“下次会话生效”不是即时推送。未初始化时由服务端安全默认人设兜底，App 仅做清晰的初始化引导。

## 下一步需求（按依赖排序）

### P0：完成可验收 V0.2 闭环

1. 用户端 B2：设备列表、绑定、详情、重命名、解绑；绑定成功后自动进入人设初始化。
2. 后端 E2 + 用户端 C1：实现并联调 persona 的 GET/PUT；补齐 unset 状态和问卷接口语义；保存后返回当前 kb_version 与 follow_latest。
3. `xiaozhi-server` 联调 persona_pack：每次会话开始拉取并应用，提供一次真实设备验收记录。

验收：新账号在 5 分钟内可认领设备、完成双鱼×INFP 设置，并在下一次实体设备对话中验证生效。

### P1：让“关系资产”可查可控

1. 后端 E4 + 用户端 C3：历史按日分页、二次确认删除、仅下发脱敏文本。
2. 后端 E5 + 用户端 C2：记忆搜索、新建、编辑、删除、candidate 通过/驳回；来源标记 manual/agent。
3. 后端 E6 + 用户端 D2：会话结束任务、日摘要/小记状态（生成中/失败/完成）与只读卡片。
4. 后端用户外设查询 + 用户端 D1：展示 emotion/gaze/closed/更新时间；页面刷新或下拉刷新拉取，不上 WebSocket。

### P2：收尾与版本演进

- 导出数据、问卷、隐私与保留策略（E8 / D3）。
- 多设备切换、通知、社交（V0.3+）；社交必须保持默认关闭和可删除的隐私控制。
- PWA 安装、离线策略与 Capacitor 包壳，均在核心闭环稳定后进行。

## 需要先钉死的契约

1. `GET /devices/{id}/persona` 的未初始化返回：建议明确 `persona_state: "unset"`，而不是以 404 表示业务状态。
2. 用户端 persona 写入和问卷提交的请求/响应 schema：sun_sign、mbti、taboo/overrides、follow_latest、kb_version。
3. `GET /devices/{id}/peripheral` 返回的字段和时间格式，及 online 的阈值定义。
4. 历史按日删除的筛选条件（device + from/to）与审计返回格式。
5. analyses 的 kind 枚举以及生成中/失败的可见状态。 

以上五项必须先更新 `ai-pet-backend/docs/06-HTTP-API规范.md`，再由前后端各自实现。

## 原型更新清单

- 已更新：高保真视觉系统、完整 P0–P8 页面、P6/P7 诚实空态、首页改为“查看设备状态”而非虚假的 App 内语音入口。
- 保留的目标态：记忆候选、按日历史、人设编辑；它们用于确认交互与信息层级，不代表当前线上可用。
- 下一轮原型：在 persona 契约定稿后，补“未初始化 → 直选/问卷 → 保存成功”的逐步状态；在 messages/memories schema 定稿后，以真实字段替换所有示例文案。
