# AI Pet 开发进度与后续计划

> 更新日期：2026-08-02（第四次复核）。依据 `D:\Home_Work\work_dashboard\AI-Pet协作看板.md`、项目全景、五仓代码与 API 契约整理。本文用于原型和产品评审；接口字段仍以 `ai-pet-backend/docs/06-HTTP-API规范.md` 为准。

## 当前结论

V0.2 的主链路已从“接口开发”进入“真实设备端到端验收”：用户可注册、以 `binding_id` 认领设备、设置人设、管理记忆、查看历史/外设/每日小记；服务端和小智侧的人设包、对话旁路、会话结束、设备首见及外设旁路代码均已部署。

当前最大的风险不是继续堆叠页面，而是证明真实设备在一次完整会话中正确写入、读取并呈现这些数据。原型应持续区分“已部署”“已真实设备验收”“等待实现”，不可把演示样例当成线上数据。

## 已部署能力与验收状态

| 域 | 已部署事实 | 仍需完成的验收 / 原型表达 |
|---|---|---|
| 账号与设备 | App 已完成登录、注册、`binding_id` 认领、设备列表/切换与当前设备持久化（B2.1/B2.2） | 用普通用户账号和真实绑定码完成一次 5 分钟认领；原型不展示 MAC 直绑 |
| 人设与角色档案 | E2、C1 已部署：12 星座、16 MBTI、忌口、钉扎；`dossier` 角色档案已写入并在下次会话编译进固定 7 字段 `persona_pack` | 真机验证保存后下一次会话的 Prompt 与 `default_emotion` 生效；保存成功页明确“等待下一次实体会话验证” |
| 小智会话链 | `pet_default` 基础行为层、首次拉取/缓存/300 秒刷新 `persona_pack`、字符串 UUID 聊天旁路、会话结束、`devices/seen`、外设快照上报均已部署；C5 动态 context API 已部署 | 以同一台真机逐项验证：首见资产、user/assistant 两条消息、断连结束、外设快照、人设变更；小智还需接 C5 并验收 Prompt 合并 |
| 对话历史 | E4、App C3 已部署：脱敏分页、按日展示、带时间窗删除与审计 | 验证旁路落库后页面真实出现记录；没有数据时显示“等待设备会话同步” |
| 记忆 | 用户/管理端 memories CRUD、候选通过/忽略及 Memory MCP 实库已部署；App C2 已接入搜索、新建、归档删除与审核 | 用真实对话产生/审核候选；明确 manual 与 agent 来源及删除影响 |
| 每日小记与成长 | LLM `daily_summary` 已首次产出；App D2 已接入 analyses 等待态与结果展示 | 验证真实会话结束后的生成时机、失败/重试状态及用户可读文案 |
| 外设状态 | 用户读取接口、App D1 与小智外设上报均已部署 | 用真实眼睛动作验证 `emotion`、`gaze`、`closed`、更新时间；App 保持只读，不直接控制 MCP |
| 管理运营 | 设备资产、绑定码轮换、人设/档案、历史、外设、分析、记忆审核及 KB 运营接口已部署 | 后续由 admin 仓完善运营界面；不成为 App 运行时依赖 |

## 明确未完成 / 不应伪装为可用

| 项目 | 当前状态 | 处理原则 |
|---|---|---|
| 数据导出 D3 | `/export` 仍为 501 | 不设计真实导出流程，仅保留规划入口 |
| 人设问卷与预览 | 后端能力仍未完成 | 不以问卷替代当前直接设置；可作为后续 onboarding 方案 |
| KB 反馈候选到草稿闭环 | 尚未闭环 | 管理端继续使用现有人工运营流程 |
| 社交、排行榜、Feed、重型养成 | V0.3+ 范围 | 不进入 V0.2 信息架构 |

## 后续开发计划

### P0：真实设备 E2E 验收（当前最高优先级）

目标：用同一真实设备和普通用户账号跑通一次可追溯闭环。

1. 设备连接后，核验 `devices/seen` 已创建/更新资产且生成可认领的 `binding_id`。
2. App 认领设备，设置“星座 + MBTI + dossier”，断开并重新发起实体会话。
3. 核验小智按 remote → cache → onboarding 降级读取 `persona_pack`，且 Prompt、`default_emotion` 已变化。
4. 核验一轮会话写入 user/assistant 两条脱敏消息；断连写入会话结束；App C3 能按日读取。
5. 执行眼睛动作，核验外设快照上报及 App D1 显示；会话结束后核验 `daily_summary` 状态。

完成标准：上述五项均有服务端记录、App 可见结果及一次人工回归记录；任一项失败均回到对应仓修复，而不是在原型中掩盖。

### P1：用户端体验收尾

1. 完善“我的星仔”：把已部署的 `dossier` 组织为用户可理解的身份、背景、关系与成长展示；首版优先允许编辑关系和陪伴偏好，基础身份/背景可先只读。
2. 完善记忆页：空态、搜索、手动新建、候选审核、归档删除的边界文案；不承诺候选会即时出现。
3. 完善小记/成长页：生成中、无数据、失败、完成四态，以及脱敏与可解释性提示。
4. 为历史、外设和小记增加真实数据为空时的可恢复空态与刷新反馈。

完成标准：核心页面在无设备、无数据、接口 404/401、生成中和已完成五类状态下均可理解、可返回，且不展示伪造的实时信息。

### P2：产品能力与运营闭环

1. 后端完成导出、人设问卷/预览，以及 KB 反馈候选→草稿闭环。
2. admin 完成角色档案、记忆审核、KB 运营和分析结果的正式界面与权限验收。
3. 核心闭环稳定后，再评估 PWA 安装、离线策略、通知与 Capacitor 包壳。
4. 社交仅在 V0.3 的隐私、开关、删除与设备发现方案评审通过后启动。

## 原型迭代建议

1. 保留现有 P3“保存 → 等待下一次实体对话验证”三态，并补充“已验证生效”状态，作为 P0 验收的可视化记录。
2. P4、P6、P7 从“接口未实现”改为“接口已部署、等待真实数据/用户验收”；示例数据必须带“设计样例”标记。
3. 增加“我的星仔 / 角色档案”页面草案前，先确认 dossier 的用户可见字段和隐私边界；不直接复用管理端字段。
4. 所有跨仓新增字段、错误语义或重试行为，先更新 backend 与小智的契约文档，再更新原型。

## 待确认契约与风险

1. 未配置人设时 `persona_pack` 的 404 与 onboarding 默认人设的固定映射。
2. 真机旁路事件的重试、去重与失败可观测性；尤其是 user/assistant 双事件和会话结束。
3. `daily_summary` 的生成时机、失败可见性、重试策略与数据保留期限。
4. dossier 中哪些字段对用户可见、哪些仅作 Prompt 编译来源。
5. 导出上线前的范围、脱敏、保留期限与用户确认流程。
